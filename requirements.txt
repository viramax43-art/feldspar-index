# Core
coqui-tts==0.27.5
aiogram==3.18.0
aiohttp>=3.9.0,<3.12
certifi>=2024.2.2
telethon==1.38.1
# AES для быстрой загрузки файлов Telethon (без него Python-AES ~100 KB/s)
cryptg>=0.4.0
PySocks>=1.7.1
pydantic-settings==2.7.1
python-dotenv==1.0.1
# coqui-tts ломается на >=5.1; RUAccent ONNX несовместим с 5.0 token_type_ids
transformers>=4.46.0,<5.0
# Required for PyTorch >= 2.9 audio IO with coqui-tts
torchcodec>=0.2.0

# Audio (librosa>=0.11 required by coqui-tts 0.27.5)
soundfile>=0.12.1,<0.15
librosa>=0.11.0,<0.12
numpy>=1.26.0,<2.2
scipy>=1.11.0,<1.16
noisereduce==3.0.3

# Text
num2words==0.5.14
ruaccent==1.5.8.3
pykakasi>=2.2.1,<3
cutlet>=0.3.0
fugashi>=1.3.0
unidic-lite>=1.0.8

# Speech-to-text (model downloads lazily on first voice message)
faster-whisper>=1.1.0,<1.3
# Neural forced-align for video dub timelines (optional: pip install whisperx)
# whisperx>=3.1.0

# Database
aiosqlite==0.20.0

# Optional: vocal/background separation for smart dubbing (pip install demucs)
demucs>=4.0.0,<5.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.3

# Optional: Telegram group/private call media (native wheels; may fail on some hosts)
# pip install "pytgcalls[telethon]"
# Private call WebRTC is best-effort; RequestCall signaling works via Telethon alone.

# PyTorch must be installed separately with CUDA (NOT the +cpu build), e.g.:
#   pip uninstall -y torch torchaudio
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# For PyTorch >= 2.9 also: pip install torchcodec
# Verify: python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
